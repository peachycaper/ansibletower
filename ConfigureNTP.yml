- name: Initial Playbook
  hosts: all
  gather_facts: no
  connection: network_cli
  ntp_server: 10.0.1.254
  tasks:
    - name: "Configure NTP"
      ios_ntp:
        server: "{{ ntp_server }}"
        logging: true
        state: present
